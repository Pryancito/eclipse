[package]
name = "hunter"
version = "0.1.0"
edition = "2018"
description = "Kernel intrusion detection and policy engine for zCore"

[dependencies]
log = "0.4"
lazy_static = { version = "1.4", features = ["spin_no_std"] }
lock = { git = "https://github.com/DeathWish5/kernel-sync", rev = "8486b8" }
