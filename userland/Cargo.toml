[package]
name = "eclipse-userland"
version = "0.1.0"
edition = "2021"
authors = ["Eclipse OS Team"]
description = "Userland de Eclipse OS en Rust"

[lib]
name = "eclipse_userland"
crate-type = ["lib"]

[dependencies]
# Dependencias básicas
libc = "0.2"
anyhow = "1.0"
log = "0.4"
env_logger = "0.10"

# Dependencias DRM
drm = "0.9"
nix = "0.27"
thiserror = "1.0"

[features]
default = []
