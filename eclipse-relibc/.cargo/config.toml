[unstable]
# Nota: no forzamos build-std aquí. Cuando hace falta (targets personalizados),
# el script de build invoca cargo con `-Z build-std=...` explícitamente.

[build]
# El JSON del target Eclipse OS está un nivel arriba (raíz del repositorio)
target = "x86_64-unknown-linux-musl"
