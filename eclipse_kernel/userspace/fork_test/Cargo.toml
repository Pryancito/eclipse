[package]
name = "fork_test"
version = "0.1.0"
edition = "2021"

[dependencies]
eclipse-libc = { package = "eclipse-libc", path = "../../../eclipse-relibc", features = ["panic-handler"] }

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
