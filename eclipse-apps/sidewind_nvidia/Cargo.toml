[package]
name = "sidewind-nvidia"
version = "0.2.0"
edition = "2021"

[dependencies]
# We will likely need heapless for fixed-size RPC buffers
heapless = "0.8"
bitflags = { version = "2.4", default-features = false }

[features]
default = ["no_std"]
no_std = []
