[binaries]
c = 'musl-gcc'
cpp = 'g++'
ar = 'ar'
strip = 'strip'
pkg-config = 'pkg-config'

[host_machine]
system = 'linux'
cpu_family = 'x86_64'
cpu = 'x86_64'
endian = 'little'

[built-in options]
# No -static en c_args (tests asm de libffi, etc.). No -static en enlace: libinput/wlroots
# necesitan libudev y otras .so del sistema; un enlace 100% estático exige sysroot musl con eudev, etc.
c_args = ['-idirafter', '/usr/include/x86_64-linux-gnu', '-idirafter', '/usr/include']
c_link_args = []
cpp_args = ['-idirafter', '/usr/include/x86_64-linux-gnu', '-idirafter', '/usr/include']
cpp_link_args = []

# Eclipse rootfs: ./build.sh ejecuta `ldd` sobre eclipse-apps/labwc/build/labwc y copia las .so a lib/.
# Alternativa: meson install a un prefijo y export ECLIPSE_LABWC_LIB_PREFIX=/ruta/prefix
# Desactivar copia ldd: ECLIPSE_SKIP_LDD_STAGING=1
