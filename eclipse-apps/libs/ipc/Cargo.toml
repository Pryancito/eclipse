[package]
name = "eclipse_ipc"
version = "0.1.0"
edition = "2021"
authors = ["Eclipse Rust Team"]
description = "Biblioteca de comunicación inter-proceso para Eclipse OS"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
tokio = { version = "1.0", features = ["net", "io-util", "rt", "time"] }
bytes = "1.0"
mio = "0.8"
