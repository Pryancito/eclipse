[toolchain]
profile = "minimal"
# `#![feature(abi_x86_interrupt)]` (src/idt.rs) keeps this on nightly; same
# channel as the workspace so one toolchain builds everything.
channel = "nightly-2026-09-01"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-uefi", "aarch64-unknown-uefi"]
